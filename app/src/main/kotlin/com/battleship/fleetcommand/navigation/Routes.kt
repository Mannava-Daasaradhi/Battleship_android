package com.battleship.fleetcommand.navigation

// Routes are defined in :core:ui — this file intentionally left empty.
// Import from com.battleship.fleetcommand.navigation (same package) works
// automatically since core:ui is on the app's compile classpath.