<?xml version="1.0" encoding="utf-8"?>
<!-- Backup rules for Android < 12 (API < 31). No app data should be backed up. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
