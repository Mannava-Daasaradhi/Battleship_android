{
  "name": "battleship-rules-tests",
  "description": "Security-rules tests for Battleship Fleet Command (Firebase RTDB emulator)",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "mocha games.rules.test.js --timeout 15000 --exit"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "mocha": "^10.0.0"
  }
}