package com.battleship.fleetcommand.core.ui.theme

import androidx.compose.ui.graphics.Color

// Deep-Navy Military Palette — Section 11
val NavyPrimary       = Color(0xFF1565C0) // Royal navy blue (brighter for better contrast)
val NavyOnPrimary     = Color(0xFFFFFFFF)
val NavySecondary     = Color(0xFF0D47A1) // Deeper navy secondary
val NavyOnSecondary   = Color(0xFFFFFFFF)
val NavyBackground    = Color(0xFF060E1A) // Near-black ocean
val NavyOnBackground  = Color(0xFFE8F0FE)
val NavySurface       = Color(0xFF0B1E35) // Slightly lighter for cards
val NavyOnSurface     = Color(0xFFE8F0FE)
val NavySurfaceVariant = Color(0xFF0F2847) // Used for elevated surfaces
val NavyError         = Color(0xFFCF6679)

// Gold accent — naval rank / prestige
val GoldAccent        = Color(0xFFD4AF37) // Antique gold
val GoldOnAccent      = Color(0xFF060E1A)

// Game-specific colours
val HitRed            = Color(0xFFFF1744) // Direct hit marker
val MissWhite         = Color(0xFFE0E0E0) // Miss marker
val SunkOrange        = Color(0xFFFF6D00) // Sunk ship highlight
val GridLine          = Color(0xFF1A3A5C) // Grid cell borders
val ValidGreen        = Color(0xFF00C853) // Valid ship placement highlight
val InvalidRed        = Color(0xFFD50000) // Invalid ship placement highlight
val FogOfWar          = Color(0xFF050C15) // Unrevealed enemy cells